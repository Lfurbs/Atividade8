#include<stdio.h>

int main(){

    int num;

    printf("Informe o numero:");
    scanf("%d", &num);

    for ( int i = 1; i <= num; i++)
    {
      if (num % i == 0)
      {
        printf("\n%d", i);
      }  
    }
}
